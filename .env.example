# Global environment variables for the cto monorepo
# Duplicate this file as `.env` at the repository root and customize values as needed.

# Base URL for the FastAPI backend used by the Flutter application
CTO_API_BASE_URL=http://localhost:8000
